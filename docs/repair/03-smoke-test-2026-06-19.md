# Hermes Android — Smoke Test Report (2026-06-19)

APK: `/storage/emulated/0/hermes 移植/hermes-agent-debug.apk`  
CI: Run #54 SUCCESS, commit `4ade904`

---

## Environment

- Package: `com.hermes.agent`
- Install command used: `pm install -r /data/local/tmp/hermes-agent-debug.apk`
- Install result: `Success`
- Existing app data: preserved (`pm install -r`)

---

## Startup / Crash Visibility

| Check | Result |
|------|--------|
| Launch `com.hermes.agent/.MainActivity` | PASS |
| Current focus after launch | `com.hermes.agent/com.hermes.agent.MainActivity` |
| `hermes_crash.log` after launch | absent (`no_crash_file`) |
| logcat fatal scan | no `FATAL EXCEPTION` / `AndroidRuntime` crash found |

---

## Page Smoke Tests

| Page | Result | Evidence |
|------|--------|----------|
| Chat | PASS | Shows `[WARN] 尚未配置供应商，请到 设置 → 模型供应商 添加`; input bar visible |
| Workflow | PASS | Shows `Live`, `历史`, `idle`, empty state, inline `启动任务` button |
| Workflow dialog | PASS | Shows `启动任务`, `Goal 循环`, `Wolfpack`, goal input, cancel/start buttons |
| Skills | PASS | Shows Skills tab, search field |
| MCP tab | PASS | Shows `MCP: 0 个服务器`, `暂无 MCP 服务器`, `点击 + 添加 MCP 服务器` |
| Add MCP dialog | PASS | Shows name field, URL field, cancel/add buttons |
| Memory | PASS | Shows `KV 记忆`, `会话历史`, search field; no crash |
| Settings | PASS | Shows providers/model/root/settings sections |
| Provider list | PASS | Shows `尚未配置任何模型供应商`, `添加供应商` |
| Root | PASS | After waiting ~7s, shows `Root 可用: false`; no infinite spinner |

---

## Static Probes Re-run

| Probe | Result |
|------|--------|
| full repo grep for unrelated path keyword | PASS |
| UI emoji unicode scan | PASS |
| Workflow event APIs present | PASS: `run_goal_async`, `run_wolfpack_async`, `drain_events` |
| Kotlin Workflow polling present | PASS: `WorkflowScreen.kt` calls `drain_events` |

---

## Not Tested — External Conditions Missing

The installed app has no configured model provider (`Provider list` is empty). Therefore these checks require a real provider/API key or user-provided MCP server:

1. `/v1/models` curl vs APP model refresh comparison
2. Real chat completion request/response
3. Wrong API key authentication error path
4. Real MCP server connection/tool count comparison
5. Workflow real model task event stream beyond launch/dialog smoke

---

## Conclusion

Smoke test passed for install, launch, crash visibility, page navigation, Workflow launch dialog, MCP empty state/add dialog, Provider empty state, and Root timeout fallback. Full end-to-end provider/MCP/Workflow validation remains blocked only by missing external credentials/server configuration.
